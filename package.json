{
  "name": "email-next-sst",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "sst": "^2.22.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.993.0",
    "aws-lambda": "^1.0.7"
  }
}
