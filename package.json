{
  "name": "email-next-sst",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/jest": "^30.0.0",
    "aws-sdk-client-mock": "^4.1.0",
    "jest": "^30.2.0",
    "sst": "^2.22.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.993.0",
    "aws-lambda": "^1.0.7"
  }
}
